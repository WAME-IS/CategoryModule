{block content}
	<h1>{$siteTitle}</h1>
	
	<p><strong>Title:</strong> {$category->langs[$lang]->title}</p>
	<p><strong>Slug:</strong> {$category->langs[$lang]->slug}</p>
	
	
	<h2>Parent</h2>
	
	{if $parent}
		<p><strong>Title:</strong> {$parent->langs[$lang]->title}</p>
		<p><strong>Title:</strong> {$parent->langs[$lang]->slug}</p>
	{/if}